<%= form_with model: [:admin, @accessory], local: true, multipart: true do |f| %>
  <div class="space-y-6 bg-white p-6 rounded-xl shadow-lg">

    <!-- TIÊU ĐỀ -->
    <h2 class="text-2xl font-semibold text-gray-700 mb-4">Thông tin phụ kiện</h2>

    <!-- Tên phụ kiện -->
    <div>
      <%= f.label :name, "Tên phụ kiện", class: "block font-medium mb-1" %>
      <%= f.text_field :name,
            class: "w-full border border-gray-300 rounded-lg px-3 py-2 focus:ring focus:ring-blue-300 focus:outline-none" %>
    </div>

    <!-- Mô tả -->
    <div>
      <%= f.label :content, "Mô tả", class: "block font-medium mb-1" %>
      <%= f.text_area :content, rows: 4,
            class: "w-full border border-gray-300 rounded-lg px-3 py-2 focus:ring focus:ring-blue-300 focus:outline-none" %>
    </div>

    <div>
      <%= f.label :images, "Ảnh", class: "block font-medium mb-1" %>
      <%= f.file_field :images, 
            multiple: true,
            class: "w-full border border-gray-300 rounded-lg px-3 py-2 bg-white" %>
    </div>

    <!-- Nút Lưu -->
    <div class="pt-4">
      <%= f.submit "Lưu phụ kiện",
            class: "w-full bg-blue-600 text-white py-3 rounded-lg font-semibold hover:bg-blue-700 transition" %>
    </div>

  </div>
<% end %>

<% if @accessory.images.attached? %>
  <div class="space-y-4 mt-6 grid grid-cols-3 gap-4">
    <% @accessory.images.each do |img| %>
      <div class="relative">
        <%= image_tag img, class: "w-48 h-48 object-cover rounded border" %>

        <%= button_to "Xóa", accessory_image_path(@accessory, img), 
              method: :delete, 
              data: { confirm: "Bạn có chắc muốn xóa ảnh này?" }, 
              class: "absolute top-1 right-1 bg-red-500 text-white px-2 py-1 rounded" %>
      </div>
    <% end %>
  </div>
<% end %>
