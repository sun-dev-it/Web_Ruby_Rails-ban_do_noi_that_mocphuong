<%= form_with model: [:admin, @category], local: true do |f| %>
  <div class="space-y-6 bg-white p-6 rounded-xl shadow-lg">

    <h2 class="text-2xl font-semibold text-gray-700 mb-4">Thông tin danh mục</h2>

    <div>
      <%= f.label :name, "Tên danh mục", class:"block font-medium mb-1" %>
      <%= f.text_field :name,
        class: "w-full border border-gray-300 rounded-lg px-3 py-2 focus:ring focus:ring-blue-300 focus:outline-none" %>
    </div>

    <div class="pt-4">
      <%= f.submit "Lưu danh mục",
        class:"w-full bg-blue-600 text-white py-3 rounded-lg font-semibold hover:bg-blue-700 transition" %>
    </div>

  </div>
<% end %>