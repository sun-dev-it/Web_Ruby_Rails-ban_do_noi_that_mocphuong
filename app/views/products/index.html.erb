<div class="mb-10 mt-4">
  <div class="px-5 py-5 text-center">
      <p class="mb-2 text-xs font-semibold uppercase tracking-[0.3em] text-gray-500">Bộ sưu tập Mộc Phương</p>
      <h1 class="my-1 text-2xl font-semibold tracking-wide text-gray-800 md:text-3xl">
        <% if params[:q].present? %>
        Kết quả tìm kiếm cho
        “<%= params[:q] %>”
        <span class="text-gray-500">(<%= @products_search.count %> sản phẩm)</span>
        <% else %>
          Tất cả sản phẩm
        <% end %>
      </h1>

      <div class="mt-3 flex items-center justify-center text-xs text-gray-500">
        <%= link_to root_path, class: "flex hover:text-black" do %>
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24"
               stroke-width="1.5" stroke="currentColor"
               class="size-3 mt-[2px]">
            <path stroke-linecap="round" stroke-linejoin="round"
              d="m2.25 12 8.954-8.955c.44-.439 1.152-.439 1.591 0L21.75 12M4.5 9.75v10.125c0 .621.504 1.125 1.125 1.125H9.75v-4.875c0-.621.504-1.125 1.125-1.125h2.25c.621 0 1.125.504 1.125 1.125V21h4.125c.621 0 1.125-.504 1.125-1.125V9.75M8.25 21h8.25" />
          </svg>
          <span>Trang chủ</span>
        <% end %>

        <span class="mx-2">›</span>
        <span>Sản phẩm</span>
        <% if params[:q].present? %>
          <span class="mx-2">›</span>
          <span>Tìm kiếm</span>
        <% end %>
      </div>
  </div>

<%= render "products_search", products_search: @products_search %>

<div class="mt-14 mb-10 text-center">
  <p class="text-xs font-semibold uppercase tracking-[0.3em] text-gray-500">Tuyển chọn Mộc Phương</p>
  <h2 class="mt-3 text-3xl font-semibold tracking-wide text-gray-900 md:text-4xl">Các sản phẩm khác</h2>

  <%= render "categories/product" %>
</div>

<div class="my-12 flex justify-center px-4 sm:my-16">
  <%= link_to categories_path,
        class: "group inline-flex items-center gap-3
                px-8 py-3
                border border-gray-300 rounded-full
                text-gray-800 font-medium
                transition-all duration-300
                hover:bg-black hover:text-white" do %>

    <span>Xem thêm sản phẩm</span>

    <!-- Arrow -->
    <svg xmlns="http://www.w3.org/2000/svg"
         viewBox="0 0 24 24" fill="none"
         stroke="currentColor" stroke-width="2"
         class="w-5 h-5
                transition-transform duration-300
                group-hover:translate-x-2">
      <path stroke-linecap="round" stroke-linejoin="round"
            d="M5 12h14M13 5l7 7-7 7" />
    </svg>

  <% end %>
</div>
